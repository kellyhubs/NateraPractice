// /** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {
    experimental: {
      appDir: true, // Enable App Directory feature
    },
};

export default nextConfig;